
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

sources:
  - name: contoso  # Logical name for your schema
    database: ADO_GROUP1_DB_ANALYSIS
    schema: CONTOSO
    tables:
      - name: Sales
        description: "Sales table in the CONTOSO schema"
      - name: Product
        description: "Product table in the CONTOSO schema"
      - name: Account
        description: "Account table in the CONTOSO schema"
    

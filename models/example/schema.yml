
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

sources:
  - name: raw
    database: ADO_GROUP1_DB_ANALYSIS   # Replace with your actual Snowflake database name
    schema: public                    # Replace with your actual schema
    tables:
      - name: sales_data              # Name of the table in Snowflake
        description: "Raw sales data before transformation"
      
    


version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

sources:
  - name: contoso               # This matches the database schema in Snowflake
    database: ADO_GROUP1_DB_ANALYSIS  # Snowflake database name
    schema: CONTOSO_SCHEMA        # Snowflake schema name
    tables:
      - name: Sales                  # Exact table name as shown in Snowflake
        description: "Sales table containing sales data"
    
